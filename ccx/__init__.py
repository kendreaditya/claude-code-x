"""ccx — claudius-code universal patcher for the Claude Code CLI binary.

A version-aware, provenance-respecting patcher. The engine is generic; patches
are pure data (registry/<group>/*.ccxpatch.json). See GOAL.md.
"""

__version__ = "0.1.0"
